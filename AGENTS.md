# StudyFlow 项目协作约定

## 进入项目后的第一步

- 每次开始处理本项目时，必须先完整阅读本文件，再查看或修改其他文件。
- 本文件适用于整个仓库；若未来子目录中出现更具体的 `AGENTS.md`，则子目录规则优先。

## 项目定位

StudyFlow 是一个面向平板和横屏设备的离线学习与练习管理 Android 应用，也是一个可公开展示、可独立解释、可维护和可测试的个人项目。

核心原则：保留真实业务问题与通用设计思路，不保留任何公司的实现、资产或身份痕迹。

## Clean-room 边界

- 所有代码、文案、图标、图片、JSON 数据、题目和设计稿必须自行创作，或使用许可明确的公开资源并记录来源。
- 禁止复制、改写或迁移公司仓库中的源代码、资源文件、接口定义、域名、包名、配置、签名信息、凭据、埋点、日志、Git 历史和内部文档。
- 禁止引入公司私有依赖、账号/设备服务、路由、Hybrid/H5、JSBridge、监控、识别/批改服务或其他内部能力。
- 包名、产品名、视觉设计、数据模型和交互文案必须独立设计，不得暗示本项目是公司产品、公司授权项目或公司代码的开源版本。
- 发现疑似敏感信息时立即停止传播，不要提交；先删除或替换，并提醒用户检查凭据轮换及保密义务。
- 简历、README 和演示中的成果只能描述本人在本仓库中实际完成并验证过的工作。

## 第一阶段：独立 MVP

优先完成一条可运行、可演示、可测试、可离线恢复的闭环：

1. 教材书架与科目卡片。
2. 教材版本选择。
3. 章节与小节列表。
4. 未开始、进行中、自批中、已完成四种练习状态。
5. 开始或继续上次学习。
6. 使用自编 JSON/Assets 提供本地 Mock 练习。
7. 提交练习并生成本地报告。
8. 使用本地持久化恢复学习进度。
9. 支持平板、横屏和基本字体缩放。

MVP 阶段不实现登录、真实后端、复杂动画、智能识别/批改、动态主题、多模块拆分、公司 SDK、Hybrid/H5、分析埋点或线上监控。

## 技术设计基线

- Kotlin + Jetpack Compose。
- 单 Activity + Navigation Compose。
- MVVM，并保持单向数据流；使用 `StateFlow` 暴露页面状态。
- Coroutines + Flow 处理异步与数据流。
- Repository 分层隔离 UI、领域规则和数据来源。
- Room 保存教材缓存、练习状态和学习进度。
- Koin 负责依赖注入。
- 页面状态统一建模为密封类型：Loading、Error、Empty、Content。
- 数据优先来自自编 JSON/Assets；只有出现明确网络需求后才引入 Retrofit、OkHttp 和序列化库，不能为了堆技术栈而添加空实现。
- 测试优先使用 JUnit、MockK 和 Compose UI Test。
- 新依赖必须是公开、必要、可解释且许可证适合公开项目。

架构可以随实际证据调整，但变更前应记录原因、替代方案和取舍，避免同时引入多个无明确收益的框架。

## 状态与业务规则

- 练习状态转换必须集中建模，不在多个页面散落条件分支。
- “继续学习”必须由持久化数据驱动，应用重启后仍能恢复。
- UI 不直接读取 JSON、Room 或网络数据，统一通过 Repository/ViewModel 获取。
- 列表项使用稳定且唯一的 key；状态更新应避免无关条目重组或刷新。
- Loading、空数据、加载失败、重试和离线数据均属于正常产品状态，必须显式设计。

## 测试与完成标准

- 每次代码变更后运行与改动范围匹配的测试或构建命令；未验证时不得声称完成。
- 优先测试：练习状态转换、进度保存与恢复、教材版本切换、JSON 到领域模型映射、缓存命中、错误重试、空数据和评分规则。
- 关键用户闭环至少包含 ViewModel 单元测试；关键页面逐步补充 Compose UI Test。
- 修复构建失败时保留并报告原始错误、配置变更和最终验证命令。
- 不提交密钥、签名文件、本机路径、IDE 临时文件、构建产物或真实用户数据。

## 第三阶段：量化优化

至少选择两项真正实现，并保留可复现的基线、实验条件、测量方法和前后数据：

- 缓存章节数据并比较请求或数据加载次数。
- 延迟显示 Loading，并统计短暂闪屏次数或首屏体验指标。
- 使用 LazyColumn 稳定 key 等方式减少无效重组。
- 验证离线状态下仍可查看教材与学习进度。
- 增加 ViewModel 单元测试，并记录核心状态分支覆盖情况。
- 统一状态机，并比较页面条件分支或非法状态数量。
- 使用 Baseline Profile 与 Macrobenchmark 测量启动或列表性能。
- 验证横屏、大屏和字体缩放适配，并记录测试设备/尺寸。

没有实际测量数据时，不在 README、简历或提交信息中写百分比提升。

## 提交与公开展示

- 提交应小而清晰，一次提交只表达一个主要意图。
- 提交前检查 `git diff`、测试结果和敏感信息。
- README 后续应包含项目定位、功能闭环、架构图、运行方式、测试方式、截图/演示、测量方法与结果，以及公开资源许可证。
- 使用截图、视频、APK 或性能数据作为可验证证据；不要用技术名词列表替代实际成果。
- 若尚未实现或验证，明确标记为计划或待办。
